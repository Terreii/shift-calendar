<%= form_with(model: school_break, class: "d-flex flex-column gap-3") do |form| %>
  <% if school_break.errors.any? %>
    <div class="alert alert-warning">
      <h2><%= pluralize(school_break.errors.count, "error") %> prohibited this school break from being saved:</h2>

      <ul>
        <% school_break.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, class: "form-label" %>
    <%= form.text_field :name, class: "form-control", required: true %>
  </div>

  <div>
    <%= form.label :start_date, class: "form-label" %>
    <%= form.date_field :start_date, class: "form-control", required: true %>
  </div>

  <div>
    <%= form.label :end_date, class: "form-label" %>
    <%= form.date_field :end_date, class: "form-control", required: true %>
  </div>

  <div>
    <%= form.submit nil, class: "btn btn-primary" %>
  </div>
<% end %>
